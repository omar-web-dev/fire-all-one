import React from 'react';
import { useLoaderData } from 'react-router-dom';
import Header from './Header'

const Home = () => {
   
    return (
        <div>
            <Header />
            
        </div>
    );
};

export default Home;